server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: redirige cualquier ruta desconocida a index.html para que Angular gestione el routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
